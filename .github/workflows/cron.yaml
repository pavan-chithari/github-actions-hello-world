name: cron job
on: 
  schedule:
    - cron: "0/1 * * * *"
  pull_request:
    type: [closed, assigned, opened, reopened]

jobs:
  cron-job:
    runs-on: ubuntu-latest
    steps:
      - name: cron job
        run: echo "Cron Job!"
        