name: Checkout
on: [push]
jobs:
  code-checkout:
    runs-on: ubuntu-latest
    steps:
      - name: check before checkout
        run: |
          pwd
          ls -a
      - name: checkout
        uses: actions/checkout@v1
      - name: check after checkout
        run: |
          pwd
          ls -a

